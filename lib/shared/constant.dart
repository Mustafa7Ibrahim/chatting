import 'package:flutter/material.dart';

const formDecoration = InputDecoration(
  hintStyle: TextStyle(
    color: Colors.grey,
    fontSize: 20.0,
  ),
);

const formPadding = const EdgeInsets.only(
  top: 18.0,
  left: 30.0,
  right: 30.0,
);
